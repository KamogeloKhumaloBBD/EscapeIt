{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "packages/db/Dockerfile.flyway"
  },
  "deploy": {
    "restartPolicyType": "NEVER"
  }
}
